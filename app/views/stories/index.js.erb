$('#content').append("<%=j render(partial: 'story', collection: @stories, format: 'html') %>");

<% if @stories.current_page == @stories.total_pages %>
  $('#view-more').remove();
<% else %>
  $('#view-more a').attr('href', '<%= url_for(page: @stories.current_page + 1) %>');
<% end %>
